# Environment probe — 2026-09-05

Manual snapshot of the dev machine (DESIGN.md §11.3). Informational only — runtime logic always goes
through `council_mcp/probe.py`, never through this file.

| Tool | Status | Notes |
|---|---|---|
| Python | 3.12.10 / 3.12.12 (Astral) via `py -3.12`; 3.14.2 default | project pinned to 3.12 (`.python-version`) |
| `uv` | 0.12.10, installed with `py -3.12 -m pip install --user uv` | lives in `%APPDATA%\Python\Python312\Scripts` — **not on PATH by default**; `.mcp.json` calls `uv` by name, so add that dir to the user PATH or install uv via winget |
| `mcp` (Python SDK) | 2.1.1 | **2.x renamed `FastMCP` → `MCPServer`** (`mcp.server.mcpserver`); DESIGN.md §0 says "FastMCP" — the server uses `MCPServer`, API is equivalent |
| `claude` | 2.1.261 | on PATH; `cheap` (claude-sub) adapter probes enabled |
| `ollama` | installed locally, `%LOCALAPPDATA%\Programs\Ollama\ollama.exe`; not auto-started | started with `ollama serve`; models: `qwen3:8b`, `qwen2.5:14b`, `qwable-9b-tools`, Qwen3.8-27B Q4/Q5, `qwen2.5vl`, `bge-m3`, `glm-ocr` — **no `qwen3-coder:30b`** |
| srv-ai Ollama (`10.20.21.9:11434`) | unreachable from this network at probe time | user decision: use local PC Ollama for now |
| Local hardware | 31 GB RAM, RTX 5070 Laptop GPU | 30B model not viable; `.council/council.json` uses `qwen3:8b`, `num_ctx` 16384; first `ask` (cold load + CLAUDE.md inlined) took 64 s |
| `gemini` CLI | missing | install: `npm i -g @google/gemini-cli` |
| `codex` CLI | missing | install: `npm i -g @openai/codex` |
| `grok` CLI | missing | Grok Build CLI not evaluated |
| `gitleaks` | missing | needed for `gates.always` later |
| `node`/`npm` | present | for CLI installs |

## Claude Code plugin format (checked 2026-09-05)

`.claude-plugin/plugin.json` + `commands/`, `agents/`, `hooks/hooks.json`, `.mcp.json` is the current
format, so DESIGN.md §2 layout stands (plan B not needed yet). Phase 0 ships only `.mcp.json`.
