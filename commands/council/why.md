---
description: Explain a task's history — every automatic decision with its reason, in ~10 lines
argument-hint: <T-001>
---

Call `council_why` with the id from $ARGUMENTS and show the lines as-is. Add one sentence of your
own only if something in the history looks wrong (e.g. a routing choice that ignored privacy).
