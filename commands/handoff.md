---
description: Write .council/HANDOFF.md for the next session (state, open blocked tasks, next steps, watch-outs)
---

Summarise this session for a fresh Claude that has none of this context: what the current epic is,
which tasks are in which state (from `council_status`), open `blocked` questions and your proposed
answers, what you planned to do next, and anything to watch out for. Keep it under 40 lines.
Call `council_handoff` with that text. Next session starts by reading HANDOFF.md, not 40 reports.
